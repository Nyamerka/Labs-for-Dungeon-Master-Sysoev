#include <stdlib.h>
#include <stdio.h>

int main () {
    int size;
    scanf("%d", &size);
    int array [size];
    for (i = 0, i < size, i++) {
        scanf("%d", &array [size]);
    }
    return 0;
}